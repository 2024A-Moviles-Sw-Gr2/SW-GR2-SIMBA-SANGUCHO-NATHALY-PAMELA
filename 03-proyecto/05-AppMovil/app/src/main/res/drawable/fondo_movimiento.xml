<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo del LinearLayout -->
    <solid android:color="@color/white" />

    <!-- Borde -->
    <stroke
        android:width="1dp"
        android:color="@color/C_dark" />

    <!-- Esquinas redondeadas -->
    <corners android:radius="18dp" />
</shape>